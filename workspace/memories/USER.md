用户偏好使用简体中文进行交流；技术术语可保留英文原文，但整体说明、总结与操作指引须用中文书写。
§
在修改、创建或删除任何代码/配置文件之前，必须先向用户说明计划：包括要改哪些文件、为什么改、具体步骤与预期结果；得到用户确认后再动手。若用户已明确授权「直接改」，仍应在执行前用简短条目列出计划。
§
默认工作目录（cwd）为 Hermes 项目根目录：JFM_HermesAgent_v0.1.0（便携部署根目录，与仓库同名）。运行终端命令、读写相对路径、引用源码与 workspace 配置时，均以此目录为基准，除非用户另行指定。
§
本项目为生产实习便携部署：运行数据在 workspace/（HERMES_HOME），网页搜索依赖本机 SearXNG（http://127.0.0.1:8888），默认 LLM 为 DeepSeek。
